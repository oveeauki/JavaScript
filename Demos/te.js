/**
@auth oveeauki
@desc JSON input & Parse
                         */
const { on } = require("events");
const {stdout,stdin,exit} = require("process");
const rl = require("readline").createInterface({
input:stdin,
output:stdout
});
const {obj} = require("./ok.json");

const obj = [

]